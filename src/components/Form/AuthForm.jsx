import classes from './authForm.module.css';

const AuthForm = ({ email, password, onEmailChange, onPasswordChange }) => {
  return (
    <div className={classes.authForm}>
      
    </div>
  )
}

export { AuthForm };

